# Manifest 规则

`manifest/` 保存来源和完整性证据，不保存生产源码、密钥、运行数据或模型权重。

每次来源采集应建立独立目录，并至少记录：采集时间、来源类型、文件相对路径、SHA-256、大小、运行状态、公开性、目标模块和导入状态。

当前 `production-baseline-2026-08-23/` 是关键资产索引，不是完整文件清单。完整清单只能在用户批准后从生产环境以只读方式生成。
